# Report: Unix-like provider API and external host instances

kind: report
spec: docs/work/unixlike/host-provider-api/spec.md
status: done

## Acceptance

| ID | State | Evidence |
| --- | --- | --- |
| AC1 | verified | The exported `lib.mkNixos`, `lib.mkDarwin`, and `lib.mkHome` are called by `provider-api-test` without importing internal files. Its synthetic NixOS, Darwin, and standalone Home outputs take their name, account, and Git values from the consumer; NixOS system and home extension modules also reach the final evaluator. |
| AC2 | verified | The constructor input schema rejects missing Git identity and host state version, an illegal machine combination, duplicate, unavailable, and unknown profiles. The existing `flake-test` still checks the five legal machine combinations and profile selection on the current outputs. |
| AC3 | verified | All seven current outputs use the exported constructors. On 2026-09-24 their toplevel derivation paths matched the clean `origin/dev` tree at `e8730ce` exactly after the constructor refactor and again after formatting. No generated-setting difference was observed in the final derivations. |
| AC4 | verified | Public `shk95/configs-host-template` pins the merged provider API `66fed098` in its synthetic `example` flake. `tool/check-hosts` evaluated its final output on aarch64 Darwin and GitHub Actions run `35979339688` passed on x86_64 Linux. Its README states the one-time copy model and the host-owned SOPS + age boundary without a real identity, recipient or secret. |
| AC5 | verified | Private `shk95/configs-hosts` PR #1 merged seven independent host flakes and locks, pinned to provider merge `66fed098`. All seven final `drvPath` values matched the `e8730ce` baseline; local `tool/check-hosts` and Linux CI run `35979483776` passed. Darwin built natively on aarch64 macOS and was permanently switched with maintainer-confirmed GUI and input runtime on 2026-09-25, recorded in private PR #10; OrbStack and UTM built natively in their aarch64 Linux guests; VMware `vm` built natively in its x86_64 guest on 2026-09-25. The OrbStack consumer was test-activated, then permanently switched and verified after an OrbStack restart on 2026-09-24. The UTM consumer was test-activated, switched, verified after reboot with maintainer-confirmed graphical behavior, then rolled back and restored on 2026-09-24. The VMware consumer was test-activated, switched, verified after reboot with maintainer-confirmed graphical behavior, then rolled back and restored on 2026-09-25; private PR #9 records that evidence. Bidirectional clipboard transfer failed on both consumer and preceding generations; the maintainer excluded that known failure from VMware completion. NixOS-WSL built its consumer natively on 2026-09-25, recorded in private `configs-hosts` PR #11. Private PR #12 records an authorized temporary test: SSH, Home Manager, DNS, Windows-started interop and the binfmt boundary held under the candidate, but `nixos-rebuild test` exited 1 for a pre-existing getty failure, and the preceding system was restored. A later clean-baseline temporary test and restoration both exited 0, recorded in private PR #13. The maintainer then authorized a permanent switch, recorded in private PR #14: running system and system profile now select the consumer, with live SSH, Home Manager, Windows interop and DNS checks passing. The distribution has not restarted, so post-restart consumer runtime remains unverified. Standalone WSL Home has native build, activation, runtime and rollback evidence on Ubuntu WSL, recorded in private PR #16. The physical desktop is uninstalled. |
| AC6 | verified | The former real `inventory.nix` is removed. `fixtures.nix` contains only `fixture-*` hosts, the `fixture-mac` Darwin host and the `example` account and Git identity. `flake-test` asserts the exact exported names, five legal NixOS machine kinds, optional profile acceptance and refusal, and no former inventory; `eval-coverage-test` refuses an empty flavour and a run that reaches no host. `provider-api-test` exercises external consumers with synthetic identities only. Native provider evaluation and CI evidence are recorded below. |
| AC7 | verified | Provider PR #390 makes host-specific Justfile selectors refuse synthetic outputs and verifies installation planning against fixtures. Repository change `27a6b15` updates README, CONTRIBUTING, `tool/doctor.sh`, the release contract and its positive and negative fixtures. After the public inventory left, the staged hygiene scan passed with `user1`, `shk`, `shk-macbook` and the personal Git address removed from its admitted names. `tool/version-control/test`, `audit --history`, `work`, `invariants` and `doctor.sh repository` passed. New Unix-like tags describe provider API and fixture evidence only; the private consumer owns final host evidence. |
| AC8 | verified | The desktop and installation specs and pending reports retain their historical verified rows and add dated ownership amendments in this increment. AC7–AC8 of the desktop and AC1 and AC6–AC10 of installation remain pending in their reports, with final instances, physical evidence and guarded deploy-rs work assigned to private `configs-hosts`. Repository change `27a6b15` revises roadmap items 9, 11, 12 and 13 to the same owner split. No physical desktop or future VM runtime is claimed. |

## Evidence lanes

- Evaluation: `provider-api-test` passes across a separate consumer flake for all three constructors and checks negative cases; all seven current output derivation paths match the clean `e8730ce` baseline. `nix flake check --no-build`, `composition-test`, and `import-order-test` pass on aarch64 Darwin. The existing local `flake-test` passed its schema fixtures, while its later installation fixture stopped because macOS `/var` is a symlink. The first Linux CI run found that the graphical runtime fixture, which composes classes without a constructor, lacked typed Home Manager identity. After that fixture supplied it, CI runs `35975584104` and final `35977694175` passed `flake-test`, full output evaluation, and `import-order-test` on x86_64 Linux.
- External template evaluation: Public template commit `dcb8c6d` pins the provider merge and passed `tool/check-hosts` locally and in Linux CI run `35979339688`; the repository is marked as a GitHub template.
- Provider-only evaluation: On 2026-09-25 the synthetic outputs evaluated as `fixture-desktop`, `fixture-orbstack`, `fixture-utm`, `fixture-vm`, `fixture-wsl`, `fixture-mac` and `example`. `nix flake check --no-build path:./unixlike` and the Alejandra formatting check passed on aarch64 Darwin. The fixture suite checks the five machine kinds, optional and unavailable profiles, rejected identities and a missing host; the evaluation coverage fixture separately rejects empty and absent outputs. The host-specific Justfile selectors refuse provider fixtures. CI evidence for the final provider-only change is added when the branch gate finishes.
- Build: The existing Darwin output built natively on aarch64 macOS after the API refactor. CI run `35975584104` built the standalone x86_64 Linux home. The private consumer's Darwin output built natively on aarch64 macOS. Its OrbStack output built on the already running aarch64 Linux machine with `nix build --no-link --max-jobs 2`; the resulting system path differed from `/run/current-system`. The UTM consumer also built its toplevel with that command inside its aarch64 NixOS guest, first in disposable mode. That test temporarily used macOS Shared Network to fetch inputs, then restored the original VM configuration byte-for-byte. After the maintainer authorized Shared as the permanent VM mode, a fresh guest boot showed a default route and successful DNS lookup. In a later non-disposable boot, the reviewed consumer flake and lock were copied into the guest with matching hashes and built natively again. The candidate differed from the preceding running system path; `nix store diff-closures` showed no package difference. After the build, `/` had 32 GiB free and `/boot` had 866 MiB free, with both generations retained. On 2026-09-25 the reviewed VMware `vm` consumer flake and lock were copied into the running x86_64 guest on the Windows host with matching hashes. A native `nix build --no-link --max-jobs 2` realised its toplevel; the candidate differed from the running and boot-profile system path. Both closures measured 10.4 GiB, with no package difference in `nix store diff-closures`. After the build, `/` had 25 GiB free and `/boot` 980 MiB. On 2026-09-25 the reviewed NixOS-WSL consumer flake and lock had matching SHA-256 values after being copied into the running x86_64 NixOS-WSL distribution. Native evaluation produced `cx1hc01f5...-nixos-system-nixos-26.11.20260916.b1b8759.drv`, and `nix build --no-link --max-jobs 2 --no-write-lock-file` realised `7hpwxkak...-nixos-system-nixos-26.11.20260916.b1b8759`. This candidate differed from the preceding running, booted and boot-profile path `ac7p0dch...`; `nix store diff-closures` showed CLI and D2Coding Nerd Font additions and Noto CJK Sans removal. `/` had 847 GiB free. Private `configs-hosts` PR #11 carries the consumer evidence. Standalone WSL Home native build is recorded below.
- Native runtime: The isolated OrbStack machine remained reachable after consumer test activation and after the later restart. On the restarted machine, its running, booted and boot-profile system links all matched the consumer candidate; `systemctl is-system-running` returned `running`, Home Manager was active, DNS worked, and no units had failed. Isolation stayed enabled, `systemd-binfmt` stayed runtime-masked, and `binfmt_misc` stayed unmounted. In UTM, key-authenticated SSH worked after test activation and again after reboot. The rebooted guest's running, booted and boot-profile system links all matched the consumer candidate; the system was `running`, sshd, greetd and Home Manager were active, DNS and the default route worked, and no units had failed. The hypervisor console displayed Niri and Noctalia after test activation. The maintainer directly confirmed Ghostty, WezTerm, Noctalia launcher and control centre, Korean input and audible playback after test activation and again after reboot. These graphical and audio observations are maintainer-confirmed, distinct from the agent's service checks. Before VMware consumer activation, its existing generation remained `running` with no failed unit; sshd, greetd, Home Manager and VMware tools were active, and the SSH route and DNS worked. After the consumer switch and reboot, a fresh key-authenticated SSH connection found the VMware guest `running`, with running, booted and boot-profile paths on the consumer candidate; sshd, greetd, Home Manager and VMware tools were active with no failed system units. Niri and Noctalia ran in the logged-in session. The maintainer directly confirmed Niri, Ghostty, WezTerm, Noctalia, Korean input, audible playback, network and pointer behavior after reboot. Standalone WSL Home runtime is recorded below. The physical desktop has no installed runtime.
- Activation: The maintainer explicitly authorized an OrbStack test activation on 2026-09-24; `nixos-rebuild test` succeeded while the boot profile retained the preceding generation. After separate authorization, `nixos-rebuild switch` succeeded and `orb restart orbstack` booted the consumer candidate. The preceding generation remains available for recovery; no rollback was needed. The maintainer authorized the UTM activation and recovery procedure. Its `nixos-rebuild test` succeeded while the boot profile retained the preceding generation. `nixos-rebuild switch` succeeded, followed by a guest reboot into the candidate. `nixos-rebuild switch --rollback` then selected the preceding generation for the running system and boot profile while SSH, greetd and Home Manager stayed active; a final consumer switch restored the candidate. No garbage collection was run. On 2026-09-25 the maintainer authorized VMware consumer test activation. `nixos-rebuild test` selected the consumer for the running system while the booted system and boot profile stayed on the preceding generation. After the clipboard failure, `nixos-rebuild test --store-path` restored the preceding running system without changing the boot profile. A fresh SSH connection found all three paths equal, the system `running`, and no failed system or user units. The maintainer then excluded the baseline clipboard failure from VMware completion and authorized the permanent transition. A repeat `nixos-rebuild test` succeeded, followed by `nixos-rebuild switch` and reboot into the consumer candidate. `nixos-rebuild switch --rollback` moved the running system and boot profile to the preceding generation while SSH, greetd, Home Manager and VMware tools stayed active with no failed system or user units. A final switch restored the consumer candidate to the running system and boot profile; the booted system already matched it. Both generations remain available, and no garbage collection was run.
- NixOS-WSL temporary native runtime: On 2026-09-25, before activation, the existing system was `degraded` solely because `getty@tty1.service` had reached its start limit; sshd and Home Manager were active. The authorized `nixos-rebuild test` selected consumer `7hpwxkak...` for `/run/current-system`, leaving `/run/booted-system` and the boot profile on preceding `ac7p0dch...`. A fresh key-authenticated SSH connection worked. sshd and Home Manager were active, the user service manager had no failed units, DNS resolved, and `binfmt_misc` remained read-only with `WSLInterop` enabled. The rendered WSL configuration still selected `user1` and enabled Windows interop; `cmd.exe /c ver` worked from a fresh Windows-started NixOS session. The same command timed out from SSH both before and during the candidate test, so SSH-session interop is not verified. The only failed system unit remained `getty@tty1.service`. Both Ubuntu and NixOS remained listed as running by Windows WSL after the test and restoration; this does not certify Ubuntu's own runtime.
- NixOS-WSL activation and restoration: The maintainer explicitly authorized only temporary test activation on 2026-09-25. `nixos-rebuild test --flake` against the reviewed consumer flake, with `--no-reexec` selected the consumer at runtime but exited 1 because `switch-to-configuration` exited 4 when the already-failed getty unit failed again. The preceding system was restored with `nixos-rebuild test --store-path /nix/store/ac7p0dch... --no-reexec`; it also exited 1 for the same getty failure. Fresh SSH confirmed running, booted and boot-profile paths all on the preceding system, sshd and Home Manager active, DNS and Windows-started interop working, and no new failed system unit. No permanent switch or WSL restart was performed. This initial test alone was qualified by the getty failure; the later clean-baseline retest is recorded below.
- NixOS-WSL clean-baseline temporary retest: Later on 2026-09-25 the preceding generation reported `running` with no failed units, and the consumer candidate was still built. Under the maintainer's temporary test authorization, `nixos-rebuild test --flake` exited 0 and made consumer `7hpwxkak...` the running system; the booted system and boot profile stayed on preceding `ac7p0dch...`. A fresh SSH connection through Windows used the NixOS host key read through the trusted Windows connection. Both system and user service managers reported `running`, sshd and Home Manager were active, no unit was failed, `binfmt_misc` stayed read-only, and a fresh Windows-started NixOS session ran `cmd.exe /c ver`. A DNS lookup for `github.com` timed out after five seconds despite a default route and WSL-generated resolver. `nixos-rebuild test --store-path` then exited 0 and restored the preceding running system. Fresh SSH confirmed all three system paths on the preceding generation, both service managers `running`, sshd and Home Manager active, and no failed unit. The same DNS lookup also timed out after five seconds on the preceding generation. Windows listed both WSL distributions as running during the retest, without establishing Ubuntu's own runtime state. No permanent switch or WSL restart was performed by this NixOS-WSL verification. Private `configs-hosts` PR #13 carries this retest; the later permanent switch is recorded below.
- NixOS-WSL permanent switch and live runtime: On 2026-09-25 the maintainer explicitly requested permanent activation. Before the switch, the preceding system reported `running` with no failed units, DNS resolved `github.com`, and the reviewed consumer flake and lock still matched their recorded SHA-256 values. `nixos-rebuild switch --flake` from the Windows host exited 0 and selected consumer `7hpwxkak...` for both `/run/current-system` and the system profile; `nixos-rebuild list-generations` marked generation 5 current. `/run/booted-system` remained on preceding `ac7p0dch...` because no WSL restart was performed. After the switch, system and user service managers reported `running`, sshd and Home Manager were active, no unit failed, `binfmt_misc` stayed read-only, DNS resolved `github.com`, and Windows-started interop worked. Fresh key-authenticated SSH through the maintainer's renamed `homewsl` alias reached NixOS and confirmed the candidate running and profile paths. Windows still listed both WSL distributions as running. DNS checks immediately before and after the permanent switch succeeded. The preceding generation remains available for recovery. No post-restart runtime or rollback from this permanent switch was exercised. Private `configs-hosts` PR #14 carries the consumer record.
- VMware clipboard exclusion: The consumer test retained fresh key-authenticated SSH, sshd, greetd, Home Manager, VMware tools and the vmblock mount. Niri exposed its virtual output; Noctalia configuration validated, and Fcitx5, PipeWire and the VMware user process ran. The maintainer directly confirmed Niri, Ghostty, WezTerm, Noctalia, Korean input, audible playback, network and pointer behavior, but found both host-to-guest and guest-to-host clipboard transfer broken. The maintainer repeated that check on the restored preceding generation and observed the same failure: this was not introduced by the consumer. The VM configuration enabled 3D acceleration and did not declare clipboard isolation flags. `open-vm-tools` 13.1.0 logs reported inability to open the old vmblock device path and uinput during session startup; their causal relationship to clipboard failure is unknown. Restarting the VMware user agent on the preceding generation did not clear those warnings or restore clipboard transfer in either direction. The [upstream release notes](https://github.com/vmware/open-vm-tools/blob/master/ReleaseNotes.md) and [open Wayland issue](https://github.com/vmware/open-vm-tools/issues/792) describe Wayland clipboard limitations. The maintainer accepted this known failure outside the VMware consumer completion criteria on 2026-09-25; clipboard functionality remains broken and is not certified by this report.
- Darwin consumer native runtime: On the aarch64 Mac, the reviewed consumer flake evaluated and built to the same system path as the current `configs` provider output. It differed from the preceding installed generation; `nix store diff-closures` showed CLI additions and no removals. After the successful switch, the running system and system profile both matched the consumer candidate, with 21 GiB free on `/` and `/nix`. Managed zsh, Zellij, PowerShell and font-configuration files matched the candidate Home Manager generation. Zoxide, Zellij and nix-index ran in an interactive terminal, and `just karabiner-check` passed. The maintainer directly confirmed Ghostty, WezTerm, new-terminal zsh and Zellij, Karabiner shortcuts and Korean input. No reboot was performed.
- Darwin activation: The maintainer authorized the consumer switch on 2026-09-25. An initial macOS administrator automation attempt advanced the system profile but failed at a `com.apple.smb.server` defaults write before completion. The profile was rolled back to the preceding generation, matching the running system. The maintainer then authenticated `sudo darwin-rebuild switch --flake` for the private consumer in a normal Terminal window. It completed Homebrew reconciliation and Home Manager activation, and selected the consumer for both running system and profile. The preceding generation remains available; a rollback from a completed consumer activation was not exercised.
- Standalone WSL Home evaluation and build: On 2026-09-25 the private consumer flake and lock were copied to Ubuntu WSL and their hashes matched the repository. `nix flake check --no-build` passed. A native `nix build --no-link --max-jobs 2` realised `/nix/store/nlfw3iv7acvcggga2jff3bggy8nnghs9-home-manager-generation` from derivation `/nix/store/212f4hw494gvx1dg0lsgfqrki3ajwyg7-home-manager-generation.drv`.
- Standalone WSL Home activation and runtime: The maintainer explicitly authorized activation. The candidate activation script succeeded. Fresh SSH found the candidate profile, user systemd `running` with no failed units, interactive Zsh, Zellij, Zoxide, nix-index and Git, valid managed Zellij configuration, enabled WSL interop and a working Windows executable. The preceding generation was activated and confirmed current, then the candidate was reactivated and confirmed current as generation 13; generation 12 retained the preceding path. A fresh connection repeated the user systemd and interop checks. No distribution reboot or kernel-global setting change was part of this validation.
- Provider-only verification: Public PR #390 passed the Unix-like CI job, including `flake-test`, full provider output evaluation and `import-order-test`, in run `36106569460`. Its merged provider flake exports five synthetic NixOS machine kinds, a synthetic Darwin output and a standalone Home output. It contains no real host inventory or final output. The private repository's README now records its seven final outputs and the provider retirement in merged PR #17.
- Review: The typed public arguments and extension points are recorded in `docs/policy/decisions/unixlike/provider-constructors-own-composition.md`. The public template and private consumer declarations are available. Installed hosts have independent consumer evidence; the uninstalled desktop retains consumer evaluation only. Secret recipients are added when a host has a verified need. Provider API coverage and final host evidence have separate owners.
