# Report: install a headless aarch64 NixOS guest on UTM

kind: report
spec: docs/work/unixlike/utm-headless-guest/spec.md
status: done

## Acceptance

| ID | State | Evidence |
| --- | --- | --- |
| AC1 | verified | Evaluation on the aarch64 Darwin host at 38e5cee: `nixosConfigurations.utm` evaluates (`as9fqcav…-nixos-system-utm`) from `nixos.utm`, `nixos.headless` and `home.shared`, with no failed assertion; `unixlike/modules/host/placeholder.nix` defines `vmware`, `orbstack` and `desktop` only. The `drvPath` of the `nixos`, `vm`, `orbstack` and `desktop` toplevels, of the standalone home's activation package and of the Darwin system, taken at 224164f before the work, are each identical at 38e5cee (`4flr7mgw…-nixos-system-nixos`, `g8zr0v6z…-nixos-system-vm`, `2vffvvvf…-nixos-system-orbstack-lxc`, `j7rzvfqb…-nixos-system-desktop`, `l5k66knp…-home-manager-generation`, `bifca69m…-darwin-system`). `unixlike/tool/checks/test` exits 0 with all seven configurations `eval ✓`; `utm` is reported `build — targets aarch64-linux`. |
| AC2 | verified | Evaluation, `unixlike/tool/checks/flake-test` at 38e5cee, in the unit tagged `INV unixlike/headless-key-only`: on `utm` the entry's account is a normal user in `wheel` with zsh as its shell and every password field null, no account carries an authorized key, sudo asks for a password, sshd is enabled on 22 with password, keyboard-interactive and root login off, the firewall is on and opens 22 alone, channels are off, and the home is the entry's account alone. Added to the real host, each of these is refused by the class's assertions: a second TCP port, a UDP port, a port opened on one interface, the firewall turned off, password authentication, a root login, an authorized key, an initial password, and a passwordless sudo; the unextended host passes clean. |
| AC3 | verified | Evaluation at 38e5cee: `flake-test`, in the unit tagged `INV unixlike/desktop-not-wsl`, finds no `wezterm` in the `utm` home's packages and finds it in the Darwin home's; `tool/checks/composition` passes over 46 feature files, none of which names a host flavour, and the new fragments read the account from `host.user`. |
| AC4 | verified | Evaluation at 38e5cee: `unixlike/modules/host/utm.nix` names the file systems by label and imports nixpkgs' QEMU guest profile; the tree holds no UUID, MAC address or generated hardware configuration for the guest, and `tool/version-control/hygiene` passes over 361 tracked paths. |
| AC5 | verified | Review at de5dcc2 (#303): `CONTRIBUTING.md` § Install the UTM guest states the installation from the aarch64 minimal ISO — the UTM machine, the two labelled file systems, the inventory entry confirmed against the medium's release, `nixos-install` from a clone, the account's password and key — and the in-place update with `just nixos-test`, `nixos-switch`, `nixos-generations` and `nixos-rollback`, and opens by saying that all of it is the maintainer's to run. The update half was exercised on this guest (AC8); the installation half was not, because the guest was adopted in place, as the spec's second amendment of 2026-09-20 records. Reviewed and confirmed by the repository maintainer on 2026-09-20. |
| AC6 | verified | Build inside the guest (aarch64 NixOS, UTM on the Mac) at 629076e: `just nixos-build utm` printed `/nix/store/pmvi10q9…-nixos-system-utm-26.11.20260916.b1b8759`. |
| AC7 | verified | Native runtime inside the guest at 629076e on 2026-09-20: `hostname` answers `utm`; `just nixos-generations` lists the generations; `sudo -k` then `sudo true` asks for the account's password, and `sudo -n true` over ssh answers `a password is required`; `nix flake metadata path:./unixlike` resolves the flake and its inputs; `nix-channel` is `command not found`, `NIX_PATH` is `nixpkgs=flake:nixpkgs` and `/etc/nixos` is absent. From the Mac: `ssh -o BatchMode=yes` with the account's key runs `hostname` and prints `utm` as `shk`, whose shell is zsh; the same connection with public-key authentication off is refused with `Permission denied (publickey)`. Read as root on the same day: `/root` holds no `.nix-channels` and no clone, and `/root/.nix-defexpr` and `/nix/var/nix/profiles/per-user/root` are empty, so nothing of the installer's channel remains. |
| AC8 | verified | Activation inside the guest on 2026-09-20, run by the repository maintainer from a clone at 629076e. `just nixos-test` then `just nixos-switch` activated `pmvi10q9…-nixos-system-utm`, generation 5, the one the first switch had made. Because the only earlier generations were the installer's, which hold no `shk` account, the rollback was exercised between two generations of this flake: with one uncommitted line in the clone's `unixlike/modules/host/utm.nix` that adds `/etc/utm-rollback-probe`, `just nixos-test` and `just nixos-switch` activated `1p1r0v8b…-nixos-system-utm` as generation 6 and the file existed; `just nixos-rollback` switched the profile from 6 to 5 and removed it; with the line reverted and the clone clean, `just nixos-switch` activated `pmvi10q9…` again as generation 7. |
| AC9 | verified | At 629076e the entry's `user` is `shk` and its `stateVersion` is `25.11`. Inside the guest `id shk` answers a normal account in `users` and `wheel`, and the release the guest was installed with, read before its first switch, was `25.11.10684.8fd9daa3db09` with `system.stateVersion = "25.11"`. `docs/status/unixlike.md` states the host as installed and names the evidence of each lane separately. Reviewed and confirmed by the repository maintainer, who ran the adoption and the activation, on 2026-09-20. |
| AC10 | verified | `Required checks` passed on the head of each pull request of this work before it merged: #297 (39599f9, merged as 224164f), #302 (4767270, merged as 54766f7) and #303 (f4e0652, merged as de5dcc2). The pull request that carries this row is held to the same check by the merge gate before it lands, and the `Work closure` run on its push is what closes #301. |
