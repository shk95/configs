# Report: update the registered NixOS-WSL distribution in place

kind: report
spec: docs/work/unixlike/nixos-wsl-in-place-update/spec.md
status: pending

## Acceptance

| ID | State | Evidence |
| --- | --- | --- |
| AC1 | verified | On x86_64-linux at the commit that turns channels off. Evaluation: `nixosConfigurations.nixos` evaluates and `config.nix.nixPath` is `nixpkgs=flake:nixpkgs` alone (`flake-test` checks it); the toplevel derivation moves from `kksxcq51…` on `dev` f2a8595 to `4flr7mgw…`, while `homeConfigurations.user1` (`l5k66knp…-home-manager-generation.drv`) and `darwinConfigurations.shk-macbook` (`bifca69m…-darwin-system-26.11.4cff07d.drv`, foreign evaluation) are the same derivations on both trees. Build: `just nixos-build` produced `/nix/store/ac7p0dch…-nixos-system-nixos-26.11.20260916.b1b8759`; the home generation built at pre-push. The Darwin toplevel is not built on this host. |
| AC2 | verified | The rendered `config.system.build.tarballBuilder` script, built on both trees: on `dev` f2a8595 it carries `nix-channel --add …/NixOS-WSL/archive/refs/heads/main.tar.gz nixos-wsl` (2 lines naming a channel); at this commit it carries none. |
| AC3 | verified | `unixlike/tool/checks/flake-test`, in the unit tagged `INV unixlike/nixos-no-channel`: a module that forces `nix.channel.enable = true` is refused by the tagged assertion, and the real configuration evaluates clean. Mutation runs, each restored: with the assertion made always true the refusal fixture fails ("was not refused"); with the option set to true the clean fixture fails. |
| AC4 | verified | On the Ubuntu distribution (x86_64-linux), at the commit that adds the recipes: `just nixos-test`, `nixos-switch`, `nixos-rollback` and `nixos-generations` each exit 1 before any `sudo` with "This host is not NixOS (/etc/NIXOS is absent)…", and `just nixos-eval` prints the toplevel derivation (`…-nixos-system-nixos-26.11.20260916.b1b8759.drv`). On the NixOS distribution (host `nixos`), run by the maintainer on 2026-09-19 19:31 KST from a clone at `dev` with the recipes: `just home-switch` exits 1 with "NixOS composes Home Manager into the system; activating the standalone home here would put the Ubuntu home over it. Use 'just nixos-switch'."; the guarded recipes pass the guard there, the host name being the flake's output name (AC6). The host-name mismatch was not exercised: there is no second NixOS host. |
| AC5 | verified | Run by the maintainer on the NixOS distribution on 2026-09-19 19:29 KST, before the cleanup step and before the first switch with channels off; the current generation was 3, built from the tree before A2. `sudo nixos-rebuild dry-build` asked for the account password, printed `building the system configuration...` and `warning: Nix search path entry '/nix/var/nix/profiles/per-user/root/channels' does not exist, ignoring`, and failed: `error: file 'nixos-config' was not found in the Nix search path (add it using $NIX_PATH or -I)`, then `Command 'nix-build '<nixpkgs/nixos>' --attr config.system.build.toplevel --dry-run' returned non-zero exit status 1.` The failure is the one the spec read from the pinned sources: safe, and unexplained to whoever follows the welcome text. |
| AC6 | verified | Run by the maintainer on the NixOS distribution on 2026-09-19, 19:29–19:33 KST. Cleanup: `sudo nix-channel --remove nixos-wsl` answered `warning: selector 'nixos-wsl' matched no installed derivations` and built a user environment (the import had added the channel and never updated it), then the two `rm` lines of the procedure ran without output. `just nixos-test` built and activated `/nix/store/ac7p0dchkflarijg74nnwyd1h466fycq-nixos-system-nixos-26.11.20260916.b1b8759` — the toplevel AC1 built on the Ubuntu distribution — restarting `home-manager-user1.service`; `just nixos-switch` then activated the same path; `just nixos-generations` lists generation 4 (2026-09-19 19:32:42, current) above 3 (2026-09-19 08:18:58), 2 and 1. `sudo nixos-rebuild switch` without `--flake` then failed on `file 'nixos-config' was not found in the Nix search path` in `nix-build '<nixpkgs/nixos>' --attr config.system.build.nixos-rebuild`, before `building the system configuration...`, and no longer warned about a channels entry, the search path carrying none. `ls /etc/nixos`: no such file or directory. At 19:45 KST `sudo ls -la /nix/var/nix/profiles/per-user/root/` lists an empty directory, last changed at 19:31 by the cleanup: root has no channel profile. |
| AC7 | pending | Read by the maintainer on the NixOS distribution after the activation of generation 4, 2026-09-19 19:33–19:34 KST: `systemctl is-system-running` and `systemctl --user is-system-running` both answer `running`; `/proc/sys/fs/binfmt_misc` holds `WSLInterop`, `python3.14`, `register` and `status`; sudo asked for the account password (AC5's run shows the prompt). Not met as read at 19:45 KST: `/mnt/c/Windows/System32/cmd.exe /c ver`, run from the maintainer's interactive zsh inside the direnv environment, failed with `/mnt/c/Windows/System32/cmd.exe: Invalid argument`. The same call, with the same trailing words, prints the Windows version on the Ubuntu distribution of the same WSL VM, where the shared `WSLInterop` registration reads `interpreter /init`, `flags: P`, so the interpreter is resolved inside each distribution. Whether the failure belongs to generation 4, to generation 3 (the nixpkgs and nixos-wsl pins moved between generations 2 and 3), or to that one shell session is not known; it is not explained by channels being off. The `findmnt` reading of the binfmt_misc mount did not run: zsh read a parenthesis in the pasted comment as a glob qualifier. Owed: `.exe` interop diagnosed and working, and the mount options. |
| AC8 | pending | The maintainer ran `just nixos-rollback` and then `just nixos-switch` on the NixOS distribution on 2026-09-19 and reports that both worked; their outputs were not kept. `just nixos-generations` afterwards lists generations 1 to 4 with 4 current and no generation 5. That is what Nix does and not a failed switch: setting a profile to the path its highest-numbered generation already holds reuses that generation, reproduced on a throwaway profile with Nix 2.35.1 on the Ubuntu distribution (set A, set B, roll back to 1, set B again: generation 2 is current again and no generation 3 appears). The criterion's "as a further generation" therefore cannot be observed when the configuration is unchanged; it awaits an amendment, and a listing taken between the rollback and the second switch would show the rollback itself. |
| AC9 | verified | What the reviewer reads: `CONTRIBUTING.md` § "Update the registered NixOS-WSL distribution" and the paragraph that links it from the import section (the procedure, the cleanup step and when its channel line must run, rollback and its window, re-import as recovery), landed by #277; `tool/doctor.sh`, which names `just nixos-test` and `just nixos-switch` on NixOS and says the standalone home is not activated there, also #277; the paragraph dated 2026-09-19 in `docs/policy/decisions/unixlike/nixos-wsl-system-layer-ownership.md` (the rejected split and its cost); and the paragraph in `docs/status/unixlike.md` that says what is declared and what the host still owes. Reviewed and confirmed by the repository maintainer on 2026-09-19, after running the procedure on the host. |
