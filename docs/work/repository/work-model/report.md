# Report: plan and verify work in documents, and keep issues for execution state

kind: report
spec: docs/work/repository/work-model/spec.md
status: done

## Acceptance

| ID | State | Evidence |
| --- | --- | --- |
| AC1 | verified | At bba7f6a: `docs/work/README.md` states the format (#266); `docs/policy/decisions/repository/work-planned-and-verified-in-documents.md`, `AGENTS.md` (Governance design), `CONTRIBUTING.md` (Branch and commit flow; "Plan and verify work") and the version-control skill (Work) state the model and the retirement of milestones. A search for `milestone` outside `docs/work/` and the superseded record finds the statements that they are no longer used, the new record's account of what it replaces, the dated account in `docs/status/repository.md`, and three past-tense accounts in accepted records and `docs/status/unixlike.md` (`unixlike-domain-owns-its-tree.md`, `design-documents-outside-the-authority-model.md`, the #192 activation paragraph); none describes milestones as the planning surface. Reviewed and confirmed by the repository maintainer on 2026-09-19. |
| AC2 | verified | `tool/version-control/test` at b49e0fb, in the unit tagged `INV repository/work-spec-has-report`: `work` refuses a spec without a report and a report without a spec (W2); a criterion with no row and a row with no criterion (W5); `done` while a criterion is not verified (W6); a spec whose scope is not its directory's and one that lists two (W3); and, with `--staged` and with `--range`, a criterion rewritten, a criterion whose lanes were weakened and a criterion removed after its report existed, as well as an amendment that names another criterion (W7). It accepts a pending pair, a carried `study` with two scopes, `done` with every row verified, `abandoned` with the unmet criterion stated, the same changes under a dated amendment naming them, and a first commit, which has no base. Mutation runs, each restored: switching off the W3 scope check, the W5 missing-row check, the W6 done check, the W7 rewrite check, the W7 removal check, or accepting any amendment, each fails the unit. |
| AC3 | verified | `tool/version-control/work` at b49e0fb: 7 work items, 3 specs paired with a report, this one and `docs-layout` included; `--range 790ca6c HEAD`, which spans the AC12 and AC4 amendments of `docs-layout` and the amendments here, passes. It runs in `.githooks/pre-commit` with `--staged` and in the CI scan job with the range under test. |
| AC4 | verified | `tool/version-control/test` at bdbb9c4, in the unit tagged `INV repository/no-closing-keyword`: each of the nine keywords, with and without a colon, before `#<n>` and before `<owner>/<repo>#<n>` (36 forms) is refused in a body, and a keyword in a subject, a capitalised one, an upper-case one and one in a merge's body are refused, each by the real `.githooks/commit-msg`, by `closing-keywords --text` (the promotion job's form) and by `closing-keywords --range … --base-ref dev` (the merge-gate step); the same range passes unread with `--base-ref master`, as the amendment of 2026-09-19 has it. `Refs #<n>`, a keyword that does not stand before the reference, a keyword that is the tail of a longer word and a `fix(...)` subject naming an issue are accepted by all three. The hook does not read comment lines or the diff below the scissors line; the text form reads every line; a range names the commit and line and counts what it read. Mutation runs, each restored: dropping `resolved`, the colon form, the `<owner>/<repo>` form, case-insensitivity or the word boundary, reading a promotion's range, reading comment lines, or no longer calling the tool from the hook, each fails the unit. On this repository, `--range origin/master origin/dev --base-ref dev` names d3046c6 and 574e0fc, which is the case the amendment was written for. The two CI steps are wired and the unit greps for them; their first real run is the pull request of this increment (the range step) and the next promotion (the body step). |
| AC5 | verified | Observed on GitHub on 2026-09-19. The push to `dev` that merged #276 (8db4713) set the docs-layout report to `done`; the `Work closure` run on that commit, https://github.com/shk95/configs/actions/runs/35436096551, logged `close-issues: 1 issue(s) closed from a report that became terminal` and closed #259 as completed with a comment linking the report at the pushed commit: https://github.com/shk95/configs/issues/259#issuecomment-5740896951. In the same run #265, whose report was pending, and #272, whose report is pending, stayed open, and `audit-remote` afterwards reported no execution issue open beside a terminal report. The four earlier runs of the workflow, from 4965c17 on, closed nothing, no report being terminal. The mechanism: the mechanism is proved by fixtures at 7772b10 with a stand-in `gh`: a report that became done closes its issue as completed with a link to the report at the pushed commit, an abandoned one as not planned, and a pending report, a report already terminal at the base, a spec naming no issue and a tree the work check refuses close nothing; five mutation runs each fail the unit. The two URLs are what the reviewer reads; the repository maintainer reviews this row in the pull request that carries it. |
| AC6 | verified | `tool/version-control/test` at 7772b10, in the unit tagged `INV repository/issue-closes-from-report`: `audit`, run in a clone that holds a pending pair past its review-by, warns `overdue spec: …/spec.md: review-by 2026-02-05 has passed`, and says that no spec is overdue when there is none; `work --unclosed`, which `audit-remote` reports as a failure per line, lists a done report whose issue a stand-in `gh` answers as open and does not list the abandoned one whose issue it answers as closed, nor a pending one. Mutation runs, each restored: ignoring the remote state, counting a pending report as terminal, or dropping the overdue reading from `audit`, each fails the unit. `audit-remote` run against the real remote at 7772b10: `no execution issue is open while its report is terminal`, 0 failures. |
| AC7 | verified | `tool/version-control/test` at b49e0fb, in the unit tagged `INV repository/design-outside-authority`: a work item's path is refused in `AGENTS.md`, `CONTRIBUTING.md`, `README.md`, `docs/policy/architecture.md`, an invariant entry, a script under `tool/`, `.githooks/`, `.github/`, `unixlike/` and `windows/`, the `README.md` line also naming the format contract beside it; it is accepted in `docs/status/`, `docs/README.md`, `docs/work/`, and, as before, in the decision, candidate and provisional inlets and the classifier's path table; the area's `README.md` and `roadmap.md` are accepted from `AGENTS.md`, an invariant entry and a script. Mutation runs, each restored: stripping every work path from a hit, removing the `docs/status` exclusion, or no longer excusing the area's files, each fails the unit. |
| AC8 | verified | `docs/work/roadmap.md` at 8f7bbf9 carries the lane table, the order table, what orders 4 to 7 carry as amended on 2026-09-19, the VMware reasoning, the binfmt constraint, and which issue each item came from. Reviewed and confirmed by the repository maintainer on 2026-09-19. |
| AC9 | verified | Done on 2026-09-19 after the maintainer confirmed the restated targets, and observed on GitHub the same day. Milestones closed: https://github.com/shk95/configs/milestone/1, https://github.com/shk95/configs/milestone/2 (#176 stays assigned to it, by the maintainer's choice) and https://github.com/shk95/configs/milestone/5. Issues closed as not planned, each with a comment linking `docs/work/roadmap.md` at 4965c17 and the item that carries it: #18 (order 4) https://github.com/shk95/configs/issues/18#issuecomment-5740456742; #23 (order 5) https://github.com/shk95/configs/issues/23#issuecomment-5740456926; #22 (order 6) https://github.com/shk95/configs/issues/22#issuecomment-5740457110; #19 (order 8) https://github.com/shk95/configs/issues/19#issuecomment-5740457260; #42 (order 8) https://github.com/shk95/configs/issues/42#issuecomment-5740457463; #20 (order 9) https://github.com/shk95/configs/issues/20#issuecomment-5740457644; #24 (order 11) https://github.com/shk95/configs/issues/24#issuecomment-5740457840; #25 (order 11) https://github.com/shk95/configs/issues/25#issuecomment-5740458016; #21 (the deferred item) https://github.com/shk95/configs/issues/21#issuecomment-5740458201; #26 (superseded by reports) https://github.com/shk95/configs/issues/26#issuecomment-5740458423. Kept open as report issues: #176, #121 and #241; the evidence #121 and #241 owe stays recorded in `docs/status/windows.md`. After the writes the open issues are #121, #176, #241 and the two execution issues #259 and #265. Reviewed and confirmed by the repository maintainer on 2026-09-19. |
| AC10 | verified | `Required checks` passed on the head of every pull request of this work before it merged: #266 (838e14d), #267 (82e4c68), #268 (a62af45), #269 (4965c17) and #270 (3b8aa61), and #276 (8db4713), which ended the docs-layout report and gave AC5 its case. The pull request that carries this row is held to the same check by the merge gate before it lands, and the `Work closure` run on its push is what closes #265. |
